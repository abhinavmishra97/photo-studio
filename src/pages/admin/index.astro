---
export const prerender = false;

import { supabaseAdmin } from "../../lib/supabaseAdmin";
import type { Enquiry } from "../../types/enquiry";
import type { Customer } from "../../types/customer";

// Fetch enquiries
let enquiries: Enquiry[] = [];
let enquiriesError: string | null = null;

try {
    const { data, error: fetchError } = await supabaseAdmin
        .from("enquiries")
        .select("*")
        .order("created_at", { ascending: false });

    if (fetchError) throw fetchError;
    enquiries = data || [];
} catch (e: any) {
    enquiriesError = e.message;
    console.error("Error fetching enquiries:", e);
}

// Fetch customers
let customers: Customer[] = [];
let customersError: string | null = null;

try {
    const { data, error: fetchError } = await supabaseAdmin
        .from("customers")
        .select("*")
        .order("created_at", { ascending: false });

    if (fetchError) throw fetchError;
    customers = data || [];
} catch (e: any) {
    customersError = e.message;
    console.error("Error fetching customers:", e);
}

// Helper functions
function formatDate(dateString: string | null | undefined): string {
    if (!dateString) return "-";
    const date = new Date(dateString);
    return date.toLocaleDateString("en-GB", {
        day: "2-digit",
        month: "2-digit",
        year: "numeric",
    });
}

function formatDateTime(dateString: string): string {
    const date = new Date(dateString);
    return date.toLocaleDateString("en-IN", {
        day: "2-digit",
        month: "short",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
    });
}

function getStatusColor(status: string): string {
    switch (status) {
        case "to_be_contacted":
            return "bg-yellow-100 text-yellow-800 border border-yellow-500 dark:bg-yellow-900/30 dark:text-yellow-900 dark:border-yellow-500";
        case "resolved":
            return "bg-green-100 text-green-800 border border-green-500 dark:bg-green-900/30 dark:text-green-900 dark:border-green-500";
        case "spam":
            return "bg-red-100 text-red-800 border border-red-500 dark:bg-red-900/30 dark:text-red-900 dark:border-red-500";
        default:
            return "bg-blue-200 text-blue-900 border border-blue-900 dark:bg-blue-900/30 dark:text-blue-400 dark:border-blue-900";
    }
}

// Get upcoming events - separated into today and future
function getUpcomingEvents() {
    const today = new Date();
    today.setHours(0, 0, 0, 0); // Start of today
    const thirtyDaysLater = new Date(today);
    thirtyDaysLater.setDate(today.getDate() + 30);

    // Helper to check if date is today
    function isToday(date: Date): boolean {
        const checkDate = new Date(date);
        return (
            checkDate.getDate() === today.getDate() &&
            checkDate.getMonth() === today.getMonth()
        );
    }

    // Filter birthdays
    const allBirthdays = customers.filter((c) => {
        if (!c.birthday) return false;
        const birthday = new Date(c.birthday);
        const thisYearBirthday = new Date(
            today.getFullYear(),
            birthday.getMonth(),
            birthday.getDate(),
        );
        return thisYearBirthday >= today && thisYearBirthday <= thirtyDaysLater;
    });

    // Separate today's birthdays from upcoming
    const todayBirthdays = allBirthdays.filter((c) => {
        const birthday = new Date(c.birthday!);
        const thisYearBirthday = new Date(
            today.getFullYear(),
            birthday.getMonth(),
            birthday.getDate(),
        );
        return isToday(thisYearBirthday);
    });

    const upcomingBirthdays = allBirthdays.filter((c) => {
        const birthday = new Date(c.birthday!);
        const thisYearBirthday = new Date(
            today.getFullYear(),
            birthday.getMonth(),
            birthday.getDate(),
        );
        return !isToday(thisYearBirthday);
    });

    // Filter anniversaries
    const allAnniversaries = customers.filter((c) => {
        if (!c.anniversary) return false;
        const anniversary = new Date(c.anniversary);
        const thisYearAnniversary = new Date(
            today.getFullYear(),
            anniversary.getMonth(),
            anniversary.getDate(),
        );
        return (
            thisYearAnniversary >= today &&
            thisYearAnniversary <= thirtyDaysLater
        );
    });

    // Separate today's anniversaries from upcoming
    const todayAnniversaries = allAnniversaries.filter((c) => {
        const anniversary = new Date(c.anniversary!);
        const thisYearAnniversary = new Date(
            today.getFullYear(),
            anniversary.getMonth(),
            anniversary.getDate(),
        );
        return isToday(thisYearAnniversary);
    });

    const upcomingAnniversaries = allAnniversaries.filter((c) => {
        const anniversary = new Date(c.anniversary!);
        const thisYearAnniversary = new Date(
            today.getFullYear(),
            anniversary.getMonth(),
            anniversary.getDate(),
        );
        return !isToday(thisYearAnniversary);
    });

    return {
        todayBirthdays,
        upcomingBirthdays,
        todayAnniversaries,
        upcomingAnniversaries,
    };
}

const {
    todayBirthdays,
    upcomingBirthdays,
    todayAnniversaries,
    upcomingAnniversaries,
} = getUpcomingEvents();
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Admin Dashboard - Ram Photo Studio</title>
    </head>

    <body
        class="bg-white dark:bg-[#1A1A1A] text-[#2A2A2A] dark:text-[#EEEAE3] min-h-screen"
    >
        <div class="max-w-7xl mx-auto px-4 py-8">
            <!-- Tab Headers -->
            <div class="mb-6">
                <div
                    class="flex gap-2 border-b border-gray-300 dark:border-gray-600"
                >
                    <button
                        id="enquiriesTab"
                        class="tab-btn px-6 py-3 font-semibold text-lg border-b-4 border-blue-500 text-blue-600 dark:text-blue-400"
                        data-tab="enquiries"
                    >
                        Enquiries ({enquiries.length})
                    </button>
                    <button
                        id="customersTab"
                        class="tab-btn px-6 py-3 font-semibold text-lg border-b-4 border-transparent text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300"
                        data-tab="customers"
                    >
                        Customers ({customers.length})
                    </button>
                </div>
            </div>

            <!-- Enquiries Content -->
            <div id="enquiriesContent" class="tab-content min-h-[600px]">
                <!-- Enquiries Header -->
                <div
                    class="mb-6 bg-gradient-to-r from-[#2563A8] to-[#1E4A8A] dark:from-[#60A5FA] dark:to-[#3B82F6] p-4 rounded-xl shadow-lg min-h-[100px]"
                >
                    <div
                        class="flex items-center justify-between flex-wrap gap-3 h-full"
                    >
                        <div>
                            <h1 class="text-4xl text-white">Enquiries</h1>
                            <div
                                class="flex flex-wrap gap-3 text-white/90 text-sm mt-2"
                            >
                                <div class="flex items-center gap-1.5">
                                    <span class="text-lg font-semibold"
                                        >{enquiries.length}</span
                                    >
                                    <span>Total</span>
                                </div>
                                <div class="h-5 w-px bg-white/30"></div>
                                <div class="flex items-center gap-1.5">
                                    <span
                                        class="text-lg font-semibold text-blue-200"
                                        >{
                                            enquiries.filter(
                                                (e) =>
                                                    e.status !==
                                                        "to_be_contacted" &&
                                                    e.status !== "resolved" &&
                                                    e.status !== "spam",
                                            ).length
                                        }</span
                                    >
                                    <span>New</span>
                                </div>
                                <div class="h-5 w-px bg-white/30"></div>
                                <div class="flex items-center gap-1.5">
                                    <span
                                        class="text-lg font-semibold text-yellow-200"
                                        >{
                                            enquiries.filter(
                                                (e) =>
                                                    e.status ===
                                                    "to_be_contacted",
                                            ).length
                                        }</span
                                    >
                                    <span>To Contact</span>
                                </div>
                                <div class="h-5 w-px bg-white/30"></div>
                                <div class="flex items-center gap-1.5">
                                    <span
                                        class="text-lg font-semibold text-green-200"
                                        >{
                                            enquiries.filter(
                                                (e) => e.status === "resolved",
                                            ).length
                                        }</span
                                    >
                                    <span>Resolved</span>
                                </div>
                                <div class="h-5 w-px bg-white/30"></div>
                                <div class="flex items-center gap-1.5">
                                    <span
                                        class="text-lg font-semibold text-red-200"
                                        >{
                                            enquiries.filter(
                                                (e) => e.status === "spam",
                                            ).length
                                        }</span
                                    >
                                    <span>Spam</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Enquiries List (Copy from enquiries.astro) -->
                {
                    enquiriesError && (
                        <div class="p-4 rounded-lg bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-700 mb-6">
                            <p class="text-red-800 dark:text-red-200 font-semibold">
                                Error loading enquiries
                            </p>
                            <p class="text-red-700 dark:text-red-300 text-sm mt-1">
                                {enquiriesError}
                            </p>
                        </div>
                    )
                }

                {
                    !enquiriesError && enquiries.length === 0 && (
                        <div class="p-8 rounded-lg bg-gray-50 dark:bg-[#26231F] border border-gray-200 dark:border-[#3A4A5E] text-center">
                            <p class="text-gray-600 dark:text-[#B8D4F0]">
                                No enquiries yet
                            </p>
                        </div>
                    )
                }

                {
                    !enquiriesError && enquiries.length > 0 && (
                        <div class="space-y-3">
                            {enquiries.map((enquiry) => (
                                <div class="rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-[#1E2838] dark:to-[#26231F] border border-blue-200 dark:border-[#3A4A5E] p-4 shadow-md hover:shadow-lg transition-all duration-200">
                                    <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-4">
                                        <div class="flex-1 min-w-0">
                                            <div class="flex items-center gap-2 mb-1 flex-wrap">
                                                <h3 class="text-lg font-semibold text-gray-900 dark:text-[#E8F1F8]">
                                                    {enquiry.name}
                                                </h3>
                                                <span
                                                    class={`px-2 py-0.5 rounded-full text-xs font-semibold ${getStatusColor(enquiry.status)}`}
                                                >
                                                    {enquiry.status}
                                                </span>
                                            </div>
                                            <p class="text-xs text-gray-600 dark:text-[#B8D4F0] mb-2">
                                                {formatDateTime(
                                                    enquiry.created_at,
                                                )}
                                            </p>
                                            <p class="text-sm text-black dark:text-[#B8D4F0] line-clamp-2 mb-3 md:mb-0">
                                                {enquiry.message}
                                            </p>
                                        </div>

                                        <div class="flex flex-col gap-3 md:items-end md:min-w-[200px]">
                                            <div class="text-left md:text-right text-xs space-y-1">
                                                {enquiry.email && (
                                                    <a
                                                        href={`https://mail.google.com/mail/?view=cm&fs=1&to=${enquiry.email}&su=Re: Your enquiry - Ram Photo Studio&authuser=ramdigitalphotostudio@gmail.com`}
                                                        target="_blank"
                                                        rel="noopener noreferrer"
                                                        class="block text-blue-700 dark:text-[#60A5FA] hover:underline truncate md:max-w-[200px]"
                                                        title={`Email ${enquiry.email}`}
                                                    >
                                                        {enquiry.email}
                                                    </a>
                                                )}
                                                <a
                                                    href={`tel:${enquiry.phone}`}
                                                    class="block text-blue-700 dark:text-[#60A5FA] hover:underline"
                                                >
                                                    {enquiry.phone}
                                                </a>
                                            </div>

                                            <div class="flex gap-1 flex-wrap md:justify-end">
                                                <button
                                                    data-enquiry-id={enquiry.id}
                                                    data-status="to_be_contacted"
                                                    class="status-btn px-2 py-1 rounded text-xs font-semibold bg-yellow-100 text-yellow-800 border border-yellow-300 dark:bg-yellow-900/30 dark:text-yellow-300 dark:border-yellow-700 hover:opacity-80 transition"
                                                    title="Mark as To Be Contacted"
                                                >
                                                    To Contact
                                                </button>
                                                <button
                                                    data-enquiry-id={enquiry.id}
                                                    data-status="resolved"
                                                    class="status-btn px-2 py-1 rounded text-xs font-semibold bg-green-100 text-green-800 border border-green-300 dark:bg-green-900/30 dark:text-green-300 dark:border-green-700 hover:opacity-80 transition"
                                                    title="Mark as Resolved"
                                                >
                                                    Resolved
                                                </button>
                                                <button
                                                    data-enquiry-id={enquiry.id}
                                                    data-status="spam"
                                                    class="status-btn px-2 py-1 rounded text-xs font-semibold bg-red-100 text-red-800 border border-red-300 dark:bg-red-900/30 dark:text-red-300 dark:border-red-700 hover:opacity-80 transition"
                                                    title="Mark as Spam"
                                                >
                                                    Spam
                                                </button>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="mt-3 pt-3 border-t border-[#A8C5E0] dark:border-[#3A4A5E]">
                                        <form
                                            class="reply-form flex flex-col sm:flex-row gap-2"
                                            data-email={enquiry.email}
                                            data-phone={enquiry.phone.replace(
                                                /\D/g,
                                                "",
                                            )}
                                            data-name={enquiry.name}
                                        >
                                            <input
                                                type="text"
                                                placeholder="Type your reply..."
                                                class="flex-1 px-3 py-2 text-sm rounded-lg border border-[#A8C5E0] dark:border-[#3A4A5E] bg-white dark:bg-[#1A2332] text-[#1A2332] dark:text-[#E8F1F8] focus:outline-none focus:border-[#2563A8] dark:focus:border-[#60A5FA]"
                                            />
                                            <div class="flex gap-2 flex-wrap">
                                                <button
                                                    type="button"
                                                    data-type="copy-email"
                                                    class="reply-btn flex-1 sm:flex-none px-3 py-2 rounded-lg bg-[#2563A8] dark:bg-[#60A5FA] text-white text-xs font-semibold hover:opacity-90 transition flex items-center justify-center gap-1"
                                                    title="Copy email text to clipboard"
                                                >
                                                    <svg
                                                        class="w-4 h-4"
                                                        fill="none"
                                                        stroke="currentColor"
                                                        viewBox="0 0 24 24"
                                                    >
                                                        <path
                                                            stroke-linecap="round"
                                                            stroke-linejoin="round"
                                                            stroke-width="2"
                                                            d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"
                                                        />
                                                    </svg>
                                                    <span class="hidden sm:inline">
                                                        Email
                                                    </span>
                                                </button>
                                                <button
                                                    type="button"
                                                    data-type="whatsapp"
                                                    class="reply-btn flex-1 sm:flex-none px-3 py-2 rounded-lg bg-[#25D366] text-white text-xs font-semibold hover:opacity-90 transition flex items-center justify-center gap-1"
                                                    title="Send via WhatsApp"
                                                >
                                                    <svg
                                                        class="w-4 h-4"
                                                        fill="currentColor"
                                                        viewBox="0 0 24 24"
                                                    >
                                                        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z" />
                                                    </svg>
                                                    <span class="hidden sm:inline">
                                                        WhatsApp
                                                    </span>
                                                </button>
                                                <button
                                                    type="button"
                                                    data-type="sms"
                                                    class="reply-btn flex-1 sm:flex-none px-3 py-2 rounded-lg bg-[#FF6B35] text-white text-xs font-semibold hover:opacity-90 transition flex items-center justify-center gap-1"
                                                    title="Send SMS/Text"
                                                >
                                                    <svg
                                                        class="w-4 h-4"
                                                        fill="none"
                                                        stroke="currentColor"
                                                        viewBox="0 0 24 24"
                                                    >
                                                        <path
                                                            stroke-linecap="round"
                                                            stroke-linejoin="round"
                                                            stroke-width="2"
                                                            d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"
                                                        />
                                                    </svg>
                                                    <span class="hidden sm:inline">
                                                        SMS
                                                    </span>
                                                </button>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                            ))}
                        </div>
                    )
                }
            </div>

            <!-- Customers Content -->
            <div id="customersContent" class="tab-content hidden min-h-[600px]">
                <!-- Customers Header -->
                <div
                    class="mb-6 bg-gradient-to-r from-[#2563A8] to-[#1E4A8A] dark:from-[#60A5FA] dark:to-[#3B82F6] p-4 rounded-xl shadow-lg min-h-[100px]"
                >
                    <div
                        class="flex items-center justify-between flex-wrap gap-3 h-full"
                    >
                        <div>
                            <h1 class="text-4xl text-white">Customer Diary</h1>
                            <p class="text-white/90 text-sm mt-1">
                                {customers.length} total customers
                            </p>
                        </div>
                        <div class="flex items-center gap-3">
                            <button
                                id="toggleOccasionsBtn"
                                class="px-4 py-2 bg-white/20 text-white border border-white/30 rounded-lg font-semibold hover:bg-white/30 transition flex items-center gap-2"
                            >
                                <svg
                                    class="w-5 h-5"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                                    ></path>
                                </svg>
                                <span id="occasionsBtnText"
                                    >View Special Occasions</span
                                >
                                <span
                                    id="occasionsCount"
                                    class="bg-white/30 px-2 py-0.5 rounded text-sm"
                                >
                                    {
                                        todayBirthdays.length +
                                            todayAnniversaries.length +
                                            upcomingBirthdays.length +
                                            upcomingAnniversaries.length
                                    }
                                </span>
                            </button>
                            <button
                                id="addCustomerBtn"
                                class="px-4 py-2 bg-white text-[#2563A8] rounded-lg font-semibold hover:bg-white/90 transition flex items-center gap-2"
                            >
                                <svg
                                    class="w-5 h-5"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M12 4v16m8-8H4"></path>
                                </svg>
                                Add Customer
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Special Occasions Section (Hidden by default) -->
                <div id="occasionsSection" class="hidden">
                    <!-- Today's Special Occasions -->
                    {
                        (todayBirthdays.length > 0 ||
                            todayAnniversaries.length > 0) && (
                            <div class="mb-6">
                                <h2 class="text-xl font-bold mb-4 text-gray-900 dark:text-white">
                                    Today's Special Occasions
                                </h2>
                                <div class="grid md:grid-cols-2 gap-4">
                                    {todayBirthdays.length > 0 && (
                                        <div class="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
                                            <h3 class="font-semibold text-gray-900 dark:text-white px-4 py-3 bg-gray-50 dark:bg-gray-800 border-b border-gray-300 dark:border-gray-600 text-base">
                                                Birthday Today
                                            </h3>
                                            <ul class="divide-y divide-gray-200 dark:divide-gray-700">
                                                {todayBirthdays.map(
                                                    (customer) => (
                                                        <li class="flex items-center justify-between gap-2 px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-800/50">
                                                            <span class="text-gray-900 dark:text-gray-100 font-medium text-base">
                                                                {customer.name}
                                                            </span>
                                                            <button
                                                                class="greet-btn px-3 py-1.5 bg-[#25D366] text-white rounded text-xs font-semibold hover:bg-[#20BA5A] transition flex items-center gap-1"
                                                                data-phone={customer.phone.replace(
                                                                    /\D/g,
                                                                    "",
                                                                )}
                                                                data-name={
                                                                    customer.name
                                                                }
                                                                data-occasion="birthday"
                                                                title="Send Birthday Wishes via WhatsApp"
                                                            >
                                                                <svg
                                                                    class="w-3 h-3"
                                                                    fill="currentColor"
                                                                    viewBox="0 0 24 24"
                                                                >
                                                                    <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z" />
                                                                </svg>
                                                                Greet
                                                            </button>
                                                        </li>
                                                    ),
                                                )}
                                            </ul>
                                        </div>
                                    )}
                                    {todayAnniversaries.length > 0 && (
                                        <div class="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
                                            <h3 class="font-semibold text-gray-900 dark:text-white px-4 py-3 bg-gray-50 dark:bg-gray-800 border-b border-gray-300 dark:border-gray-600 text-base">
                                                Anniversary Today
                                            </h3>
                                            <ul class="divide-y divide-gray-200 dark:divide-gray-700">
                                                {todayAnniversaries.map(
                                                    (customer) => (
                                                        <li class="flex items-center justify-between gap-2 px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-800/50">
                                                            <span class="text-gray-900 dark:text-gray-100 font-medium text-base">
                                                                {customer.name}
                                                            </span>
                                                            <button
                                                                class="greet-btn px-3 py-1.5 bg-[#25D366] text-white rounded text-xs font-semibold hover:bg-[#20BA5A] transition flex items-center gap-1"
                                                                data-phone={customer.phone.replace(
                                                                    /\D/g,
                                                                    "",
                                                                )}
                                                                data-name={
                                                                    customer.name
                                                                }
                                                                data-occasion="anniversary"
                                                                title="Send Anniversary Wishes via WhatsApp"
                                                            >
                                                                <svg
                                                                    class="w-3 h-3"
                                                                    fill="currentColor"
                                                                    viewBox="0 0 24 24"
                                                                >
                                                                    <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z" />
                                                                </svg>
                                                                Greet
                                                            </button>
                                                        </li>
                                                    ),
                                                )}
                                            </ul>
                                        </div>
                                    )}
                                </div>
                            </div>
                        )
                    }

                    <!-- Upcoming Occasions (Next 30 Days) -->
                    {
                        (upcomingBirthdays.length > 0 ||
                            upcomingAnniversaries.length > 0) && (
                            <div class="mb-6">
                                <h2 class="text-lg font-semibold mb-3 text-gray-700 dark:text-gray-300">
                                    Upcoming Occasions (Next 30 days)
                                </h2>
                                <div class="grid md:grid-cols-2 gap-4">
                                    {upcomingBirthdays.length > 0 && (
                                        <div class="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
                                            <h3 class="font-semibold text-gray-900 dark:text-white px-4 py-3 bg-gray-50 dark:bg-gray-800 border-b border-gray-300 dark:border-gray-600 text-base">
                                                Birthdays
                                            </h3>
                                            <ul class="divide-y divide-gray-200 dark:divide-gray-700">
                                                {upcomingBirthdays.map(
                                                    (customer) => (
                                                        <li class="flex items-center justify-between gap-2 px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-800/50">
                                                            <span class="text-gray-900 dark:text-gray-100 font-medium text-base">
                                                                {customer.name}{" "}
                                                                -{" "}
                                                                {formatDate(
                                                                    customer.birthday,
                                                                )}
                                                            </span>
                                                            <button
                                                                class="greet-btn px-2 py-1 bg-[#25D366] text-white rounded text-xs hover:bg-[#20BA5A] transition"
                                                                data-phone={customer.phone.replace(
                                                                    /\D/g,
                                                                    "",
                                                                )}
                                                                data-name={
                                                                    customer.name
                                                                }
                                                                data-occasion="birthday"
                                                                title="Send Birthday Wishes via WhatsApp"
                                                            >
                                                                Greet
                                                            </button>
                                                        </li>
                                                    ),
                                                )}
                                            </ul>
                                        </div>
                                    )}
                                    {upcomingAnniversaries.length > 0 && (
                                        <div class="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
                                            <h3 class="font-semibold text-gray-900 dark:text-white px-4 py-3 bg-gray-50 dark:bg-gray-800 border-b border-gray-300 dark:border-gray-600 text-base">
                                                Anniversaries
                                            </h3>
                                            <ul class="divide-y divide-gray-200 dark:divide-gray-700">
                                                {upcomingAnniversaries.map(
                                                    (customer) => (
                                                        <li class="flex items-center justify-between gap-2 px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-800/50">
                                                            <span class="text-gray-900 dark:text-gray-100 font-medium text-base">
                                                                {customer.name}{" "}
                                                                -{" "}
                                                                {formatDate(
                                                                    customer.anniversary,
                                                                )}
                                                            </span>
                                                            <button
                                                                class="greet-btn px-2 py-1 bg-[#25D366] text-white rounded text-xs hover:bg-[#20BA5A] transition"
                                                                data-phone={customer.phone.replace(
                                                                    /\D/g,
                                                                    "",
                                                                )}
                                                                data-name={
                                                                    customer.name
                                                                }
                                                                data-occasion="anniversary"
                                                                title="Send Anniversary Wishes via WhatsApp"
                                                            >
                                                                Greet
                                                            </button>
                                                        </li>
                                                    ),
                                                )}
                                            </ul>
                                        </div>
                                    )}
                                </div>
                            </div>
                        )
                    }
                </div>
                <!-- End Special Occasions Section -->

                <!-- Search Bar and Sort -->
                <div class="mb-4 flex gap-3">
                    <input
                        type="text"
                        id="searchInput"
                        placeholder="Search by name or phone..."
                        class="flex-1 px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 focus:outline-none focus:border-blue-500"
                    />
                    <select
                        id="sortSelect"
                        class="px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 focus:outline-none focus:border-blue-500"
                    >
                        <option value="newest">Newest First</option>
                        <option value="oldest">Oldest First</option>
                    </select>
                </div>

                <!-- Customers Table (Copy from customers.astro) -->
                {
                    customersError && (
                        <div class="p-4 rounded-lg bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-700 mb-6">
                            <p class="text-red-800 dark:text-red-200 font-semibold">
                                Error loading customers
                            </p>
                            <p class="text-red-700 dark:text-red-300 text-sm mt-1">
                                {customersError}
                            </p>
                        </div>
                    )
                }

                {
                    !customersError && customers.length === 0 && (
                        <div class="p-8 rounded-lg bg-gray-50 dark:bg-[#26231F] border border-gray-200 dark:border-[#3A4A5E] text-center">
                            <p class="text-gray-600 dark:text-[#B8D4F0]">
                                No customers yet. Click "Add Customer" to get
                                started!
                            </p>
                        </div>
                    )
                }

                {
                    !customersError && customers.length > 0 && (
                        <div class="overflow-x-auto">
                            <table class="w-full bg-white dark:bg-gray-800 rounded-lg shadow-md">
                                <thead class="bg-gray-100 dark:bg-gray-700">
                                    <tr>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Name
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Spouse Name
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Phone
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Birthday
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Anniversary
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Locality
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            City
                                        </th>
                                        <th class="px-4 py-3 text-left text-base font-semibold">
                                            Notes
                                        </th>
                                        <th class="px-4 py-3 text-center text-base font-semibold">
                                            Actions
                                        </th>
                                    </tr>
                                </thead>
                                <tbody id="customersTableBody">
                                    {customers.map((customer) => (
                                        <tr
                                            class="border-t border-gray-200 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-700/50 customer-row"
                                            data-customer-id={customer.id}
                                        >
                                            <td class="px-4 py-3 text-base font-medium text-gray-900 dark:text-gray-100">
                                                {customer.name}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300">
                                                {customer.spouse_name || "-"}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300">
                                                {customer.phone}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300">
                                                {formatDate(customer.birthday)}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300">
                                                {formatDate(
                                                    customer.anniversary,
                                                )}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300 max-w-xs truncate">
                                                {customer.location || "-"}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300">
                                                {customer.city || "-"}
                                            </td>
                                            <td class="px-4 py-3 text-base text-gray-700 dark:text-gray-300 max-w-xs truncate">
                                                {customer.notes || "-"}
                                            </td>
                                            <td class="px-4 py-3 text-center">
                                                <div class="flex items-center justify-center gap-2">
                                                    <button
                                                        class="edit-btn px-3 py-1 bg-blue-500 text-white rounded text-xs hover:bg-blue-600 transition"
                                                        data-customer={JSON.stringify(
                                                            customer,
                                                        )}
                                                    >
                                                        Edit
                                                    </button>
                                                    <button
                                                        class="delete-btn px-3 py-1 bg-red-500 text-white rounded text-xs hover:bg-red-600 transition"
                                                        data-customer-id={
                                                            customer.id
                                                        }
                                                        data-customer-name={
                                                            customer.name
                                                        }
                                                    >
                                                        Delete
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    ))}
                                </tbody>
                            </table>
                        </div>
                    )
                }
            </div>
        </div>

        <!-- Customer Modal (same as before) -->
        <div
            id="customerModal"
            class="hidden fixed inset-0 bg-black/50 flex items-center justify-center p-4 z-50 overflow-y-auto"
        >
            <div
                class="bg-white dark:bg-gray-800 rounded-lg shadow-xl max-w-md w-full p-6 my-8 max-h-[90vh] overflow-y-auto"
            >
                <h2
                    id="modalTitle"
                    class="text-2xl font-bold mb-4 text-gray-900 dark:text-white sticky top-0 bg-white dark:bg-gray-800 pb-2 border-b border-gray-200 dark:border-gray-700"
                >
                    Add Customer
                </h2>

                <form id="customerForm" class="space-y-4">
                    <input type="hidden" id="customerId" />

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Name *</label
                        >
                        <input
                            type="text"
                            id="customerName"
                            required
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="Customer name"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Spouse Name</label
                        >
                        <input
                            type="text"
                            id="customerSpouseName"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="Spouse/partner name"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Phone *</label
                        >
                        <input
                            type="tel"
                            id="customerPhone"
                            required
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="+91 XXXXX XXXXX"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Birthday</label
                        >
                        <input
                            type="date"
                            id="customerBirthday"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Anniversary</label
                        >
                        <input
                            type="date"
                            id="customerAnniversary"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >City</label
                        >
                        <input
                            type="text"
                            id="customerCity"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="City name"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Locality</label
                        >
                        <input
                            type="text"
                            id="customerLocation"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="Locality/area"
                        />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold mb-1"
                            >Notes</label
                        >
                        <textarea
                            id="customerNotes"
                            rows="3"
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 focus:outline-none focus:border-blue-500"
                            placeholder="Brief notes about customer..."
                        ></textarea>
                    </div>

                    <div class="flex gap-3 pt-2">
                        <button
                            type="submit"
                            id="saveBtn"
                            class="flex-1 px-4 py-2 bg-blue-500 text-white rounded-lg font-semibold hover:bg-blue-600 transition"
                        >
                            Save
                        </button>
                        <button
                            type="button"
                            id="cancelBtn"
                            class="flex-1 px-4 py-2 bg-gray-300 dark:bg-gray-600 text-gray-800 dark:text-white rounded-lg font-semibold hover:bg-gray-400 dark:hover:bg-gray-500 transition"
                        >
                            Cancel
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Delete Confirmation Modal -->
        <div
            id="deleteModal"
            class="hidden fixed inset-0 bg-black/50 flex items-center justify-center p-4 z-50"
        >
            <div
                class="bg-white dark:bg-gray-800 rounded-lg shadow-xl max-w-md w-full p-6"
            >
                <h2
                    class="text-2xl font-bold mb-4 text-gray-900 dark:text-white"
                >
                    Confirm Delete
                </h2>
                <p class="text-gray-700 dark:text-gray-300 mb-6">
                    Are you sure you want to delete <span
                        id="deleteCustomerName"
                        class="font-semibold"></span>? This action cannot be
                    undone.
                </p>
                <div class="flex gap-3 justify-end">
                    <button
                        id="cancelDeleteBtn"
                        class="px-4 py-2 bg-gray-300 dark:bg-gray-600 text-gray-800 dark:text-gray-200 rounded-lg font-semibold hover:bg-gray-400 dark:hover:bg-gray-500 transition"
                    >
                        Cancel
                    </button>
                    <button
                        id="confirmDeleteBtn"
                        class="px-4 py-2 bg-red-500 text-white rounded-lg font-semibold hover:bg-red-600 transition"
                    >
                        Delete
                    </button>
                </div>
            </div>
        </div>

        <script is:inline>
            // Tab switching with URL hash support
            const tabBtns = document.querySelectorAll(".tab-btn");
            const tabContents = document.querySelectorAll(".tab-content");

            function switchTab(tabName) {
                // Update button styles
                tabBtns.forEach((b) => {
                    b.classList.remove(
                        "border-blue-500",
                        "text-blue-600",
                        "dark:text-blue-400",
                    );
                    b.classList.add(
                        "border-transparent",
                        "text-gray-500",
                        "dark:text-gray-400",
                    );
                });

                const activeBtn = document.querySelector(
                    `[data-tab="${tabName}"]`,
                );
                if (activeBtn) {
                    activeBtn.classList.add(
                        "border-blue-500",
                        "text-blue-600",
                        "dark:text-blue-400",
                    );
                    activeBtn.classList.remove(
                        "border-transparent",
                        "text-gray-500",
                        "dark:text-gray-400",
                    );
                }

                // Show/hide content
                tabContents.forEach((content) => {
                    if (content.id === `${tabName}Content`) {
                        content.classList.remove("hidden");
                    } else {
                        content.classList.add("hidden");
                    }
                });

                // Update URL hash
                window.location.hash = tabName;
            }

            // Handle tab button clicks
            tabBtns.forEach((btn) => {
                btn.addEventListener("click", () => {
                    const tabName = btn.dataset.tab;
                    switchTab(tabName);
                });
            });

            // Load tab from URL hash on page load
            window.addEventListener("DOMContentLoaded", () => {
                const hash = window.location.hash.substring(1); // Remove #
                if (hash === "customers" || hash === "enquiries") {
                    switchTab(hash);
                }
            });
        </script>

        <script is:inline src="/admin-dashboard.js"></script>
    </body>
</html>
